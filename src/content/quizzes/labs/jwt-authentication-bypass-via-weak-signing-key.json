{
  "questions": [
    {
      "q": "What does HS256 stand for?",
      "o": ["High-Security 256-bit encryption", "HMAC with SHA-256", "Hash-Salt 256", "Hybrid Signing 256-bit mode"],
      "a": 1,
      "x": "HS256 is HMAC-SHA256. It uses a single shared secret to both create and verify the signature. Anyone who knows the secret can forge valid tokens.",
      "h": "HS and 256 are two separate abbreviations. What common MAC algorithm does HS stand for, and what does 256 refer to?"
    },
    {
      "q": "Why can JWT secret cracking be done entirely offline, without sending any requests to the server?",
      "o": ["The server caches verification results for each token", "The token itself contains the header, payload, and signature needed to test any candidate secret", "JWT servers have a key-recovery endpoint", "The secret is embedded in the JWKS endpoint"],
      "a": 1,
      "x": "HMAC verification only needs the algorithm, the data (header.payload), and the key. All of that is visible in the captured JWT. An attacker can test candidates locally at GPU speed without touching the server.",
      "h": "What does HMAC verification need as inputs? Are all those inputs present in the token itself?"
    },
    {
      "q": "What hashcat mode is used to crack JWT HS256 secrets?",
      "o": ["Mode 1000 (NTLM)", "Mode 16500 (JWT)", "Mode 3200 (bcrypt)", "Mode 18300 (Apple Keychain)"],
      "a": 1,
      "x": "Hashcat mode 16500 is specifically designed for JWTs. It accepts the full token, tries each wordlist entry as the HMAC secret, and reports a hit when the computed signature matches.",
      "h": "Hashcat has a dedicated mode for JWTs. Its number is in the range 16000-17000."
    },
    {
      "q": "What wordlist is typically used to crack JWT secrets in this class of attack?",
      "o": ["rockyou.txt, because it contains real leaked passwords", "jwt.secrets.list from SecLists, because it focuses on common JWT signing secrets found in tutorials and configs", "A random hex string dictionary", "The target application's source code"],
      "a": 1,
      "x": "jwt.secrets.list is a curated list of secrets commonly found in JWT tutorials, official docs examples, and leaked configurations. Developers copy these strings directly without changing them.",
      "h": "General password wordlists work, but there is a purpose-built list specifically for JWT secrets in the SecLists project."
    },
    {
      "q": "Once the secret is cracked, what do you need to do to forge an admin token?",
      "o": ["Submit the secret to the server's /authenticate endpoint", "Modify the payload (change sub to administrator) and re-sign it with the cracked secret using HS256", "The cracked secret unlocks a hidden admin route directly", "Decode the token, remove the signature, and add a custom admin header"],
      "a": 1,
      "x": "With the secret you can produce a valid HMAC signature for any payload you choose. Change sub to administrator, sign the new header.payload with HS256 and the cracked secret, and the server will accept the token as legitimate.",
      "h": "Knowing the secret means you can produce a valid signature. What changes do you need in the payload to gain admin access?"
    },
    {
      "q": "What is the minimum entropy recommended for a production HS256 JWT secret?",
      "o": ["8 printable characters (password-style)", "At least 256 bits of cryptographically random entropy", "Any string longer than 20 characters", "A UUID (128-bit)"],
      "a": 1,
      "x": "The secret should have at least as much entropy as the hash output, which is 256 bits for SHA-256. A 32-byte output from a CSPRNG satisfies this. Human-readable strings almost never reach this bar.",
      "h": "The number in HS256 is a hint. How many bits of entropy should a secret have to match the strength of the underlying hash?"
    },
    {
      "q": "Why is a secret copied from a tutorial or README dangerous?",
      "o": ["Tutorials use keys that expire after a fixed date", "The same string appears in public wordlists and will be cracked immediately", "Tutorial secrets use a weak algorithm by default", "They are encrypted with a public key everyone can download"],
      "a": 1,
      "x": "Strings like 'your-secret-key', 'supersecret', or 'jwt_secret' are included in every JWT-focused wordlist because developers routinely paste them from documentation without changing them.",
      "h": "What happens to a secret that thousands of developers have copied from the same docs page? Is it still secret?"
    },
    {
      "q": "After cracking the secret, which claim in the payload is changed to access /admin?",
      "o": ["alg", "iat", "sub", "kid"],
      "a": 2,
      "x": "The sub (subject) claim holds the authenticated username. Changing it to 'administrator' and re-signing with the cracked secret produces a token the server accepts as a valid admin session.",
      "h": "You need to impersonate a different user. Which JWT claim identifies who is authenticated?"
    },
    {
      "q": "What is the primary advantage of switching from HS256 to RS256 for JWT signing?",
      "o": ["RS256 tokens are shorter and faster to transmit", "The signing key is a private key that never leaves the server, so even a leaked public key cannot forge tokens", "RS256 does not use a shared secret at all, so there is nothing to crack", "RS256 is immune to algorithm confusion attacks"],
      "a": 1,
      "x": "RS256 uses an asymmetric key pair. The private key signs and stays on the server. The public key verifies and can be shared openly. Even if an attacker gets the public key, they cannot sign new tokens without the private key.",
      "h": "What is the difference between a symmetric and asymmetric signing approach? Who can create vs. verify with each?"
    },
    {
      "q": "Where should a production JWT secret be stored?",
      "o": ["Hardcoded in the source code for easy deployment", "In an environment variable or a secrets manager, never in source code", "In the JWT header for easy retrieval", "In the database alongside user records"],
      "a": 1,
      "x": "Hardcoded secrets end up in version control and are exposed to anyone with repository access. Environment variables and secrets managers (like Vault, AWS Secrets Manager) keep them out of code and logs.",
      "h": "What storage location keeps a secret out of version control, logs, and compiled binaries?"
    }
  ]
}
